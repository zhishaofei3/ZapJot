{
  "name": "@zapjot/extension",
  "version": "1.0.0",
  "description": "ZapJot Chrome Extension - Lightning-fast notes with tabs and categories",
  "scripts": {
    "clean": "rm -rf dist && mkdir dist",
    "package": "npm run clean && mkdir -p release && cp manifest.json popup.html popup.css popup.js dist/ && cp -r icons dist/ && cd dist && zip -r ../release/zapjot-extension.zip .",
    "dev": "node dev-server.js"
  },
  "keywords": [
    "chrome-extension",
    "notepad",
    "popup"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "chokidar": "^3.5.3"
  }
}
